# ⚠️ SECURITY NOTICE

**DO NOT commit sensitive credentials to Git!**

This `.env.example` file contains placeholder values. Copy this file to `.env` and replace all placeholders with your actual credentials.

```bash
cp .env.example .env
```

Then edit `.env` with your actual values.

---

# Django Settings
DEBUG=True
SECRET_KEY=<generate-a-long-random-secret-key>
ALLOWED_HOSTS=*

# Database Configuration
DATABASE_URL=postgres://<username>:<password>@localhost:5432/vehic_aid

# Redis Configuration
REDIS_URL=redis://localhost:6379/1
CELERY_BROKER_URL=redis://localhost:6379/0
CHANNEL_REDIS_URL=redis://localhost:6379/1

# Email Configuration (Gmail SMTP)
# Get App Password from: https://myaccount.google.com/apppasswords
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=<your-email@gmail.com>
EMAIL_HOST_PASSWORD=<your-16-digit-app-password>
DEFAULT_FROM_EMAIL=VehicAid <<your-email@gmail.com>>
SERVER_EMAIL=<your-email@gmail.com>

# SMS Configuration (Fast2SMS)
# Get API Key from: https://www.fast2sms.com/dashboard/dev-api
SMS_PROVIDER=fast2sms
FAST2SMS_API_KEY=<your-fast2sms-api-key>

# Payment Gateway (Razorpay)
# Get credentials from: https://dashboard.razorpay.com/
RAZORPAY_KEY_ID=<your-razorpay-key-id>
RAZORPAY_KEY_SECRET=<your-razorpay-key-secret>

# Google Maps API
# Get API Key from: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=<your-google-maps-api-key>

# Firebase Configuration
# Get config from: https://console.firebase.google.com/
FIREBASE_CONFIG='{"apiKey": "<your-api-key>", "authDomain": "<your-project-id>.firebaseapp.com", "projectId": "<your-project-id>", "storageBucket": "<your-project-id>.firebasestorage.app", "messagingSenderId": "<your-sender-id>", "appId": "<your-app-id>"}'

---

## 🔐 Security Best Practices

1. **Never commit `.env` file to Git** - It's already in `.gitignore`
2. **Use strong, unique passwords** for all services
3. **Enable 2FA** on all service accounts
4. **Rotate credentials regularly** (every 90 days)
5. **Use different credentials** for development and production
6. **Keep this file secure** - Don't share it publicly

## 📚 Setup Guides

- **Email Setup**: See `docs/SMTP_SETUP.md`
- **SMS Setup**: See `docs/FREE_SMS_SETUP.md`
- **Payment Setup**: See Razorpay documentation
- **Maps Setup**: See Google Cloud Console documentation
- **Firebase Setup**: See Firebase Console documentation

## 🆘 Need Help?

If you need help setting up any service:
1. Check the documentation in the `docs/` folder
2. Visit the service provider's documentation
3. Contact the development team

---

**Last Updated**: January 17, 2026
